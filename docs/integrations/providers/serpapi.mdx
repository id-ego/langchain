---
description: LangChain에서 SerpAPI 검색 API를 사용하는 방법에 대한 설치, 설정 및 래퍼에 대한 정보를 제공합니다.
---

# SerpAPI

이 페이지는 LangChain 내에서 SerpAPI 검색 API를 사용하는 방법을 다룹니다. 설치 및 설정과 특정 SerpAPI 래퍼에 대한 참조로 두 부분으로 나뉩니다.

## 설치 및 설정
- `pip install google-search-results`로 요구 사항 설치
- SerpAPI API 키를 얻고 이를 환경 변수(`SERPAPI_API_KEY`)로 설정

## 래퍼

### 유틸리티

이 API를 래핑하는 SerpAPI 유틸리티가 존재합니다. 이 유틸리티를 가져오려면:

```python
<!--IMPORTS:[{"imported": "SerpAPIWrapper", "source": "langchain_community.utilities", "docs": "https://api.python.langchain.com/en/latest/utilities/langchain_community.utilities.serpapi.SerpAPIWrapper.html", "title": "SerpAPI"}]-->
from langchain_community.utilities import SerpAPIWrapper
```


이 래퍼에 대한 더 자세한 안내는 [이 노트북](/docs/integrations/tools/serpapi)을 참조하세요.

### 도구

이 래퍼를 도구로 쉽게 로드할 수도 있습니다(에이전트와 함께 사용).
다음과 같이 할 수 있습니다:
```python
<!--IMPORTS:[{"imported": "load_tools", "source": "langchain.agents", "docs": "https://api.python.langchain.com/en/latest/agent_toolkits/langchain_community.agent_toolkits.load_tools.load_tools.html", "title": "SerpAPI"}]-->
from langchain.agents import load_tools
tools = load_tools(["serpapi"])
```


이에 대한 더 많은 정보는 [이 페이지](/docs/how_to/tools_builtin)를 참조하세요.