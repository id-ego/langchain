---
description: Airbyte는 API, 데이터베이스 및 파일에서 데이터 웨어하우스 및 데이터 레이크로 ELT 파이프라인을 위한 데이터 통합
  플랫폼입니다.
---

# 에어바이트

> [에어바이트](https://github.com/airbytehq/airbyte)는 API, 데이터베이스 및 파일에서 데이터 웨어하우스 및 데이터 레이크로의 ELT 파이프라인을 위한 데이터 통합 플랫폼입니다. 데이터 웨어하우스 및 데이터베이스에 대한 가장 큰 ELT 커넥터 카탈로그를 보유하고 있습니다.

## 설치 및 설정

```bash
pip install -U langchain-airbyte
```


:::note

현재 `langchain-airbyte` 라이브러리는 Pydantic v2를 지원하지 않습니다.
이 패키지를 사용하려면 Pydantic v1로 다운그레이드하십시오.

이 패키지는 또한 현재 Python 3.10+가 필요합니다.

:::

통합 패키지는 설정해야 할 전역 환경 변수가 필요하지 않지만, 일부 통합(예: `source-github`)은 전달된 자격 증명이 필요할 수 있습니다.

## 문서 로더

### AirbyteLoader

[사용 예시](/docs/integrations/document_loaders/airbyte)를 참조하십시오.

```python
from langchain_airbyte import AirbyteLoader
```