---
description: Wolfram Alpha API를 LangChain에서 사용하는 방법과 설치, 설정, 유틸리티 및 도구에 대한 정보를 제공합니다.
---

# Wolfram Alpha

> [WolframAlpha](https://en.wikipedia.org/wiki/WolframAlpha)는 `Wolfram Research`에서 개발한 답변 엔진입니다.  
사실적인 질문에 대해 외부 소스 데이터를 계산하여 답변합니다.

이 페이지는 LangChain 내에서 `Wolfram Alpha API`를 사용하는 방법을 다룹니다.

## 설치 및 설정
- 다음을 사용하여 요구 사항을 설치합니다.  
```bash
pip install wolframalpha
```
  
- Wolfram Alpha에 가서 개발자 계정을 [여기](https://developer.wolframalpha.com/)에서 등록합니다.  
- 앱을 생성하고 `APP ID`를 받습니다.  
- 환경 변수 `WOLFRAM_ALPHA_APPID`로 APP ID를 설정합니다.

## 래퍼

### 유틸리티

이 API를 래핑하는 WolframAlphaAPIWrapper 유틸리티가 존재합니다. 이 유틸리티를 가져오려면:  
```python
<!--IMPORTS:[{"imported": "WolframAlphaAPIWrapper", "source": "langchain_community.utilities.wolfram_alpha", "docs": "https://api.python.langchain.com/en/latest/utilities/langchain_community.utilities.wolfram_alpha.WolframAlphaAPIWrapper.html", "title": "Wolfram Alpha"}]-->
from langchain_community.utilities.wolfram_alpha import WolframAlphaAPIWrapper
```
  

이 래퍼에 대한 더 자세한 설명은 [이 노트북](/docs/integrations/tools/wolfram_alpha)을 참조하세요.

### 도구

이 래퍼를 도구로 쉽게 로드할 수도 있습니다(에이전트와 함께 사용).  
다음과 같이 할 수 있습니다:  
```python
<!--IMPORTS:[{"imported": "load_tools", "source": "langchain.agents", "docs": "https://api.python.langchain.com/en/latest/agent_toolkits/langchain_community.agent_toolkits.load_tools.load_tools.html", "title": "Wolfram Alpha"}]-->
from langchain.agents import load_tools
tools = load_tools(["wolfram-alpha"])
```
  

도구에 대한 더 많은 정보는 [이 페이지](/docs/how_to/tools_builtin)를 참조하세요.