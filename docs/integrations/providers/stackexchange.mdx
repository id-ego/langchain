---
description: Stack Exchange API를 LangChain에서 사용하는 방법에 대한 문서입니다. 설치, 유틸리티 및 도구 사용법을
  다룹니다.
---

# 스택 익스체인지

> [스택 익스체인지](https://en.wikipedia.org/wiki/Stack_Exchange)는 다양한 분야의 주제에 대한 질문과 답변(Q&A) 웹사이트 네트워크로, 각 사이트는 특정 주제를 다루며, 질문, 답변 및 사용자는 평판 보상 프로세스의 적용을 받습니다.

이 페이지는 LangChain 내에서 `Stack Exchange API`를 사용하는 방법을 다룹니다.

## 설치 및 설정
- 요구 사항을 설치하려면 
```bash
pip install stackapi
```


## 래퍼

### 유틸리티

이 API를 래핑하는 StackExchangeAPIWrapper 유틸리티가 존재합니다. 이 유틸리티를 가져오려면:

```python
<!--IMPORTS:[{"imported": "StackExchangeAPIWrapper", "source": "langchain_community.utilities", "docs": "https://api.python.langchain.com/en/latest/utilities/langchain_community.utilities.stackexchange.StackExchangeAPIWrapper.html", "title": "Stack Exchange"}]-->
from langchain_community.utilities import StackExchangeAPIWrapper
```


이 래퍼에 대한 자세한 안내는 [이 노트북](/docs/integrations/tools/stackexchange)을 참조하세요.

### 도구

이 래퍼를 도구로 쉽게 로드할 수도 있습니다(에이전트와 함께 사용).
다음과 같이 할 수 있습니다:
```python
<!--IMPORTS:[{"imported": "load_tools", "source": "langchain.agents", "docs": "https://api.python.langchain.com/en/latest/agent_toolkits/langchain_community.agent_toolkits.load_tools.load_tools.html", "title": "Stack Exchange"}]-->
from langchain.agents import load_tools
tools = load_tools(["stackexchange"])
```


도구에 대한 자세한 정보는 [이 페이지](/docs/how_to/tools_builtin)를 참조하세요.